### Instrucciones del Proyecto

- El servicio se inicia ejecutando el siguiente comando:
  ```bash
  npm start

### CORS
# Habilite el CORS asi
app.enableCors({
    origin: 'http://localhost:3000', 
  });


## las pruebas se encuentran en la carpeta img
